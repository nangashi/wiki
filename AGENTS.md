## 行動方針

### 全体
- 忖度しない: ページ品質・スキル実行判断・提案で問題があれば率直に指摘する

### insightコレクション
- 核心を明確にする: この概念が何を説明し、どんな推論に使えるかを明確にする
- 有用な情報に絞る: 核心の理解と適用判断に必要な情報に絞る。必要な定義・適用条件・具体例は残す

### itコレクション
- 集約を最優先: 新規ページより既存技術ページへの統合を優先する（1技術1ページ）。分割などの例外は `wiki/it/schema.md` に従う
- 記事として読める品質: queryで引くだけでなく、通読して技術を体系的に理解できる状態を保つ
- 判断材料を残す: 比較・採用判断・アンチパターン・トレードオフを優先して取り込む

## 統括と委譲

- Astraが統括する。依頼の解釈、作業分割、採用・統合先の判断、成果物の最終確認、終了判定を担当する。
- 品質を維持しながら実行コストを抑えるため、作業に合う役割・モデルへ自律的に委譲する。独立評価など必須の分離を除き、依頼・確認の負担に見合う作業を委譲する。
- 狭い検索・候補抽出・資料整理は `finder`（Luna）、指定範囲の執筆・修正は `editor`（Terra）、独立評価・事実検証は `evaluator`（Sol）を使う。モデルと推論強度は `.codex/agents/` を正本とし、未指定の子がAstraを継承しないよう `.codex/config.toml` に既定値を置く。
- カスタム役割を直接選べないクライアントでは、対応するTOMLを読み、モデル・推論強度・担当指示を明示して標準サブエージェントを起動する。役割名をタスク名に付けるだけでモデルが切り替わるとは見なさない。
- 単発コマンドや小さな修正は直接処理する。役割を毎回一式起動しない。機械検査は既存スクリプトを使い、モデルで再計算しない。
- 子には対象、必要な背景・制約・基準、編集可能なファイル、成果物、完了条件を含む自己完結した依頼を渡す。通常は会話履歴を引き継がない起動（対応ツールでは `fork_turns="none"`）を使う。
- 子の報告は指定された出力schemaを優先し、必須項目を省略しない。指定がなければ結論、根拠の場所、変更ファイル、検証結果、未解決点を簡潔に返す。親は同じ探索を繰り返さず、判断に必要な原文と差分を確認する。
- 子からの再委譲は原則行わない。並行する子は最大3件。同じファイルへの書き込み担当は一人とし、index生成、評価manifestの更新は親が調整して直列化する。
- 下位モデルの結果が不十分なら不足箇所を絞って修正を依頼し、繰り返し失敗する判断はAstraが引き取る。ただし記事の独立評価はSolに固定し、Astraや執筆担当が判定や根拠を代行・補完しない。
- 委譲による総トークン削減は保証されない。重複読込、長い報告、不要な再試行を減らし、品質を保つ。通常報告に使用量の長い内訳を加える必要はない。

## リポジトリ構成と実行

- このリポジトリはCodexのみで運用し、通常の委譲はCodex標準のサブエージェント機能を使う。
- スキル定義の正本は `.agents/skills/` のみとし、別ディレクトリへ複製しない。
- コレクション構成は `wiki/collections.md`、保存形式は各 `schema.md`、insightの品質基準と評価手順は `wiki/insight/references/` を必要に応じて読む。これらの基準をエージェント定義へ複製しない。
- insightの独立評価は `wiki/insight/references/evaluation-protocol.md` に従う。新しい履歴なしの `evaluator` を使い、前回評価結果・執筆会話は渡さず、記事を変更させない。起動・代替手段・保存・終了条件は同プロトコルを参照し、実行できなければ評価を代行せず制約を報告する。
- サブエージェントの権限は実行環境の制約にも従う。read-only設定を理由に、利用中の権限上書きを無視してよいと解釈しない。
- indexのサマリは `.agents/skills/lint/wiki_structure.py index` で記事の概要から生成する。被リンクは同スクリプトの `backlinks <collection>:<slug>` で取得し、相互リンクのためだけに関連先を書き換えない。
