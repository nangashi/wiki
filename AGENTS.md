## 行動方針

### 全体
- 忖度しない: ページ品質・スキル実行判断・提案で問題があれば率直に指摘する

### insightコレクション
- 本質を最優先: 「この概念の核心は何か」を繰り返し自問し、答えが出てから書く
- 有用な情報に絞る: 本質と無関係な説明・具体例は含めない

### itコレクション
- 集約を最優先: 新規ページより既存技術ページへの統合を優先する（1技術1ページ）
- 記事として読める品質: queryで引くだけでなく、通読して技術を体系的に理解できる状態を保つ
- 判断材料を残す: 比較・採用判断・アンチパターン・トレードオフを優先して取り込む

## 統括と委譲

- Astraが統括する。依頼の解釈、作業分割、採用・統合先の判断、成果物の最終確認、終了判定を担当する。
- **トークン節約を目的に、作業内容に応じたモデルを選択してサブエージェントに委譲する。利用者から毎回依頼されるのを待たず、適した独立作業は自律的に委譲する。**
- 狭い検索・候補抽出・資料整理は `finder`（Luna）、指定範囲の執筆・修正は `editor`（Terra）、独立採点・事実検証は `evaluator`（Sol）を使う。モデルと推論強度は `.codex/agents/` を正本とし、未指定の子がAstraを継承しないよう `.codex/config.toml` に既定値を置く。
- カスタム役割を直接選べないクライアントでは、対応するTOMLを読み、モデル・推論強度・担当指示を明示して標準サブエージェントを起動する。役割名をタスク名に付けるだけでモデルが切り替わるとは見なさない。
- 単発コマンドや小さな修正は直接処理する。役割を毎回一式起動しない。機械検査は既存スクリプトを使い、モデルで再計算しない。
- 子には対象、必要な基準、編集可能なファイル、成果物、完了条件だけを渡す。通常は会話履歴を引き継がない起動（対応ツールでは `fork_turns="none"`）を使い、必要な文脈を明示する。
- 子は結論、根拠の場所、変更ファイル、検証結果、未解決点を簡潔に返す。親は同じ探索を繰り返さず、判断に必要な原文と差分を確認する。採点の必須出力schemaは省略しない。
- 子からの再委譲は原則行わない。並行する子は最大3件。同じ記事への書き込み担当は一人とし、index生成、評価manifestの更新は親が調整して直列化する。
- 下位モデルの結果が不十分なら不足箇所を絞って修正を依頼し、繰り返し失敗する判断はAstraが引き取る。ただし記事の独立採点はSolに固定し、Astraや執筆担当が点数を代行・補完しない。
- 委譲による総トークン削減は保証されない。重複読込、長い報告、不要な再試行を減らし、品質を保つ。通常報告に使用量の長い内訳を加える必要はない。

## リポジトリ構成と実行

- このリポジトリはCodexのみで運用する。`AGENTS.md` は実ファイルであり、基本方針の正本とする。
- スキル定義の正本は `.agents/skills/` のみ。`.claude/skills/` や別ディレクトリへ複製しない。Codexでは `$query`・`$ingest`・`$review-page`・`$lint`、または自然言語で呼び出す。
- コレクション構成は `wiki/collections.md`、保存形式は各 `schema.md`、insightの品質基準と評価手順は `wiki/insight/references/` を必要に応じて読む。これらの基準をエージェント定義へ複製しない。
- 通常の委譲はCodex標準のサブエージェント機能を使う。Claude Codeのツール、プラグイン、バックグラウンドBash起動には依存しない。
- 独立評価は `wiki/insight/references/evaluation-protocol.md` に従い、新しい履歴なしの `evaluator` に対象記事と基準を渡す。前回点数・執筆会話は渡さず、記事を変更させない。標準サブエージェントで履歴分離やread-onlyを設定できない場合だけ、同プロトコルの独立CLI実行へ切り替える。どちらも使えなければ採点を代行せず、制約を報告する。
- サブエージェントの権限は実行環境の制約にも従う。read-only設定を理由に、利用中の権限上書きを無視してよいと解釈しない。
- indexのサマリは `.agents/skills/lint/wiki_structure.py index` で記事の概要から生成する。被リンクは同スクリプトの `backlinks <collection>:<slug>` で取得し、相互リンクのためだけに関連先を書き換えない。
