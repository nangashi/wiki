# it記事のレビュー

対象記事と `../schema.md`、[記事品質基準](../references/article-quality-rubric.md)を読む。I1〜I4を順に確認し、関連先・類似技術は必要な範囲を読む。他wikiの評価プロトコルは適用しない。「評価のみ」の明示指定では提案までとし、記事・indexを変更しない。

改善は事実誤り・再現不能 → 運用移転性 → 理解の自立性 → リンク → 統合・分割の順に提示する。通常の本文・リンク修正も方針を示して承認後に適用し、`updated` を更新して `python3 tools/wiki/wiki_structure.py index --collection it` でindexを生成する。統合・分割・削除・リダイレクト化は必ず個別確認を取る。

## 完了報告

```markdown
## レビュー結果: [[it:<slug>]] — タイトル
| 基準 | 判定 | 詳細 |
|---|---|---|
| I1: 理解の自立性 | 合格 / 不合格 | ... |
| I2: 運用移転性 | 合格 / 不合格 | ... |
| I3: リンク品質 | 合格 / 改善あり | ... |
| I4: 統合・分割 | 保持 / 統合候補 / 分割候補 | ... |

### 改善提案（優先度順）
1. ...
```
